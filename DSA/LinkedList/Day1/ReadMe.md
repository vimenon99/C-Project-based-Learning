# Linked List — Day 1

### 🧠 Concepts Learned
- What a **Linked List** and **Node** are in C  
- Creating and initializing a linked list using `malloc`  
- Appending new nodes to the end  
- Traversing and printing the list contents  

### ⚙️ Functions Implemented
- `createList()` → Initializes an empty linked list  
- `createNode(int val)` → Creates a new node with given data  
- `append(LinkedList* list, int val)` → Adds a node at the end of the list  
- `traverseNodes(LinkedList* list)` → Prints all nodes in order  

