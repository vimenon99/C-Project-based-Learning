# Linked List — Day 3

### 🧩 Tasks Covered
1. **Reverse the Linked List**
   - Reversed the node links using three pointers (`prev`, `curr`, `next`).
   - Updated `head` to point to the new first node.


2. **Search in Linked List**
   - Traversed through nodes to find a specific value.
   - Returned index if found, otherwise indicated not found.


